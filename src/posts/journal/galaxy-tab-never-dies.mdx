---
title: "갤럭시탭 넌 죽지못해"
date: "2026-01-03"
description: "termux활용하여 유휴 갤럭시탭 서버로 활용하기"
tags: [termux, android, galaxy-tab, home-server, 재활용]
published: true
---

# 갤럭시탭 넌 죽지못해

서랍 속에서 먼지만 쌓이고 있는 갤럭시탭, 혹시 있으신가요? 저도 그랬습니다. 새 태블릿을 사고 나면 이전 기기는 자연스럽게 방치되기 마련이죠. 하지만 이 녀석들, 아직 쓸모가 남아있습니다. 바로 **Termux**를 활용한 홈 서버로 재탄생시키는 것입니다.

---

## 1. Termux란?

**Termux**는 안드로이드 기기에서 리눅스 환경을 제공하는 터미널 에뮬레이터입니다. 루팅 없이도 사용할 수 있으며, apt 패키지 매니저를 통해 다양한 리눅스 패키지를 설치할 수 있습니다.

주요 특징:

- **루팅 불필요**: 일반 앱처럼 설치하고 바로 사용
- **패키지 매니저 지원**: `pkg` 혹은 `apt` 명령어로 패키지 관리
- **SSH, Python, Node.js, Git 등**: 개발 환경 구축 가능
- **proot-distro**: Ubuntu, Debian 등 다양한 리눅스 배포판 설치 가능

공식 사이트: [https://termux.dev](https://termux.dev)

---

## 2. Termux 설치 방법

### ⚠️ 중요: Google Play 스토어 버전은 사용하지 마세요!

Google Play의 Termux는 **2020년 이후 업데이트가 중단**되었습니다. 오래된 버전은 패키지 설치 시 오류가 발생하고, 보안 취약점이 존재할 수 있습니다.

### ✅ 권장: F-Droid에서 설치

[F-Droid](https://f-droid.org)는 오픈소스 앱 마켓으로, Termux 개발자가 직접 최신 버전을 배포합니다.

**설치 순서:**

1. **알 수 없는 출처 앱 허용**
   - `설정` → `보안` (또는 `개인정보 보호`) → `알 수 없는 출처` 활성화

2. **F-Droid 설치**
   - 브라우저에서 [f-droid.org](https://f-droid.org) 접속
   - `Download F-Droid` 버튼으로 APK 다운로드 → 설치

3. **Termux 검색 및 설치**
   - F-Droid 앱 실행 → "Termux" 검색 → 설치

> **Tip**: Termux:API, Termux:Styling 같은 애드온도 F-Droid에서 함께 설치하면 좋습니다.

---

## 3. Termux 초기 세팅

설치가 끝났다면, 먼저 패키지를 업데이트하고 SSH 서버를 설정합니다.

### 3-1. 패키지 업데이트

```bash
# 저장소 변경 (미러 선택 화면이 나오면 가까운 지역 선택)
termux-change-repo

# 패키지 업데이트
pkg update && pkg upgrade -y
```

### 3-2. 기본 패키지 설치

```bash
# 자주 사용하는 기본 도구들
pkg install -y openssh vim git curl wget
```

### 3-3. SSH 서버 설정 (sshd)

외부에서 태블릿에 접속하려면 SSH 서버가 필요합니다.

```bash
# SSH 서버 시작 (기본 포트: 8022)
sshd

# 비밀번호 설정
passwd
```

**접속 방법:**

```bash
# 다른 기기에서 접속 (태블릿 IP 확인: ifconfig)
ssh -p 8022 사용자명@태블릿IP
```

> **참고**: Termux의 SSH는 8022 포트를 사용합니다 (기본 22번 아님).

### 3-4. 부팅 시 자동 시작

Termux:Boot 앱을 설치하면 기기 재부팅 후 자동으로 sshd를 실행할 수 있습니다.

```bash
# ~/.termux/boot/ 디렉토리 생성
mkdir -p ~/.termux/boot

# 자동 시작 스크립트 작성
echo "sshd" > ~/.termux/boot/start-sshd.sh
chmod +x ~/.termux/boot/start-sshd.sh
```

---

## 4. Termux로 이런 것도 가능해요

Termux만으로도 다양한 프로젝트를 진행할 수 있습니다. 별도의 리눅스 배포판 설치 없이 네이티브하게 사용할 수 있는 활용 사례들을 소개합니다.

### 🐍 Python 개발 환경

```bash
pkg install python
pip install flask requests numpy
```

- **웹 서버 구축**: Flask, FastAPI로 간단한 API 서버
- **자동화 스크립트**: 파일 정리, 데이터 수집, 알림 봇
- **IoT 연동**: MQTT 클라이언트로 스마트홈 제어

📎 [Termux Python 가이드](https://wiki.termux.com/wiki/Python)

### 🟢 Node.js 서버

```bash
pkg install nodejs
npm install -g http-server
```

- **로컬 웹 서버**: 정적 파일 호스팅
- **Express.js API**: REST API 서버 구축
- **개발 테스트**: 프론트엔드 프로젝트 로컬 테스트

📎 [Termux Node.js 위키](https://wiki.termux.com/wiki/Node.js)

### 🗄️ 데이터베이스

```bash
pkg install mariadb   # MySQL 호환
pkg install postgresql
pkg install redis
```

- 로컬 개발용 DB 서버로 활용
- 간단한 데이터 저장소

📎 [Termux MariaDB 가이드](https://wiki.termux.com/wiki/MariaDB)

### 🌐 웹 서버

```bash
pkg install nginx
pkg install apache2
```

- 정적 웹사이트 호스팅
- 리버스 프록시 설정

📎 [Termux Nginx 위키](https://wiki.termux.com/wiki/Nginx)

### 📡 네트워크 도구

```bash
pkg install nmap      # 네트워크 스캐닝
pkg install traceroute
pkg install netcat-openbsd
```

- 네트워크 진단 및 보안 테스트
- 포트 스캐닝, 연결 테스트

### 🔧 개발 도구

```bash
pkg install git vim tmux
pkg install clang      # C/C++ 컴파일러
pkg install rust       # Rust 개발
pkg install golang     # Go 개발
```

- Git 서버로 활용 (git daemon)
- 다양한 언어 개발 환경

📎 [Termux 개발 환경 구축](https://wiki.termux.com/wiki/Development_Environments)

### 📱 Termux:API 활용

F-Droid에서 **Termux:API** 앱을 추가로 설치하면:

```bash
pkg install termux-api

# 사용 예시
termux-battery-status    # 배터리 상태
termux-sms-send          # SMS 전송
termux-notification      # 알림 표시
termux-camera-photo      # 카메라 촬영
termux-location          # 위치 정보
```

스크립트와 연동하면 강력한 자동화가 가능합니다!

📎 [Termux:API 문서](https://wiki.termux.com/wiki/Termux:API)

---

## 마무리

이렇게 해서 서랍 속 갤럭시탭이 리눅스 서버로 다시 태어났습니다. 가벼운 홈 자동화 서버, Git 서버, 개발 테스트 환경 등 다양하게 활용할 수 있습니다.

다음 글에서는 이 환경 위에 특정 서비스를 배포하는 방법을 다뤄보겠습니다.

갤럭시탭, 이제 진짜 죽지 못합니다. 🔋
